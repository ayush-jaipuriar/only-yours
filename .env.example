# ─────────────────────────────────────────────────────────────────
# Only Yours — Environment Variables Template
#
# Copy this file to .env and fill in the values for your environment.
# The .env file is NEVER committed to git (see .gitignore).
#
# For production: set these in your container environment or
# secrets manager (AWS Secrets Manager, Google Secret Manager, etc.)
# ─────────────────────────────────────────────────────────────────

# PostgreSQL Connection
DATABASE_URL=jdbc:postgresql://localhost:5432/onlyyours
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your_strong_database_password_here

# JWT Signing Key
# Must be a strong random value — at least 256 bits (32 bytes)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_256_bit_base64_encoded_jwt_secret_here

# Google OAuth 2.0 Web Client ID (optional — Google sign-in is currently disabled)
# Obtain from: Google Cloud Console → APIs & Services → Credentials
# GOOGLE_CLIENT_ID=your_project_id.apps.googleusercontent.com

# Resend API Key (for transactional email — password reset)
# Obtain from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here

# Resend "from" sender email.
# No-domain test mode (free): onboarding@resend.dev (usually only to your own verified email).
# After domain verification: use noreply@yourdomain.com
RESEND_FROM_EMAIL=onboarding@resend.dev

# Spring Profile (optional — defaults to no profile for local dev)
# Set to 'prod' for production deployments
SPRING_PROFILES_ACTIVE=prod
